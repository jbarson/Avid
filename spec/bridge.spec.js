describe('Bridge exists', function () {

  it('BRIDGE should exist', function () {
    expect(BRIDGE).toBeDefined();
  });
  it('BRIDGE.ships should exist', function () {
      expect(BRIDGE.ships).toBeDefined();
  });
});